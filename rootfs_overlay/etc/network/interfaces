# interface file auto-generated by buildroot

auto lo
iface lo inet loopback

auto eth0
#allow-hotplug eth0
iface eth0 inet static
  address 172.17.0.1
  netmask 255.255.255.0

auto wlan0
	iface wlan0 inet static
	address 172.17.0.1
	netmask 255.255.255.0
	broadcast 172.17.0.255
        up /sbin/hostapd /etc/hostapd.conf &

# Firewall zur??cksetzen, Tabellen leeren
#	up /sbin/iptables -F
#	up /sbin/iptables -X
#	up /sbin/iptables -t nat -F

# Kabelnetzwerk maskieren, Port-Forwarding sowie Nat aktivieren
#	up iptables -A FORWARD -o eth0 -i wlan0 -s 192.168.1.0/24 -m conntrack --ctstate NEW -j ACCEPT
#	up iptables -A FORWARD -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
#	up iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
#	up sysctl -w net.ipv4.ip_forward=1
#	up echo 1 > /proc/sys/net/ipv4/ip_forward

# hostapd und dnsmasq (neu) starten
#	up service hostapd restart
#	up service dnsmasq restart
